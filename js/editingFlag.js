import React from 'react';

export default function EditingFlag({ mode }) {

    if ( mode === 'edit') {
        return ( <span className="inner-title">Editing    </span> )
    }
    else {
        return
    }
}